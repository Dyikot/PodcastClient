@page "/signin"
@inject NavigationManager NavigationManager
@inject IDbContextFactory<ApplicationDbContext> DbContextFactory

<PageTitle>@Resource.SignIn</PageTitle>

<div class="layout p-2 d-flex align-items-center justify-content-center">
	<EditForm class="formLayout bg-1 border-main rounded-2 p-3"
			  Model="User"
			  OnValidSubmit="OnValidSubmit">
		<DataAnnotationsValidator />
		<h4>@Resource.SignIn</h4>
		<div class="w-100">
			<label for="username" class="form-label fontSize-1">
				@Resource.UserName
			</label>
			<InputText id="username" class="form-control" @bind-Value="User.UserName" />
			<ValidationMessage For="@(() => User.UserName)" />
		</div>
		<div class="w-100">
			<label for="password" class="form-label fontSize-1">
				@Resource.Password
			</label>
			<InputText type="password" id="password" class="form-control"
					   @bind-Value="User.Password" />
			<ValidationMessage For="@(() => User.Password)" />
		</div>
		@if(_errorMessage != null)
		{
			<span class="validation-message">@_errorMessage</span>			
		}
		<button class="mt-1 w-100 btn btn-warning text-black" type="submit">
			@Resource.Submit
		</button>
		<p class="mt-1">
			<span>@Resource.NoAccount</span>
			<a href="signup">@Resource.DoSignUp</a>
		</p>
	</EditForm>
</div>
